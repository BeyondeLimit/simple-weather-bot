export class BotInformationService {
    public getWelcomeText = () => {
        return`
        Welcome to the simple weather bot 🤖
    Use /help command for list of features (or use the Menu box)
        `;
    }
}